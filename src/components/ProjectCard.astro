---
const { id, title, description, techs, img, url, github } = Astro.props
---

<!-- <li class="project-card-container scroll-reveal"> -->
<li class="project-card scroll-reveal">
  <header><h3 class="project-title">{title}</h3></header>
  <main><p>{description}</p></main>
  <div class="image-container">IMAGEN</div>
  <footer>
    <a title="Go to the site's URL" href="" class="project-link"
      >View Live Site</a
    >
    <a title="Go to the site's code on GitHub" href="" class="project-link"
      >View GitHub Repo</a
    >
  </footer>
</li>
<!-- </li> -->

<style>
  /* .project-card-container {
    transition: transform 0.4s ease-in;
    transform: translateY(20px) scale(0);
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: space-evenly;
  } */

  /* .project-card-container::after {
    transform: scale(0);
    transition:
      transform 2s ease-in,
      width 2s ease-in;
  }
  .project-card-container.active {
    transform: translateY(0px) scale(1);
    position: relative;
    max-width: 80ch;
  }
  .project-card-container.active::after {
    content: '';
    transform: scale(1);
    background: linear-gradient(90deg, #61dafb, #ef652a);
    position: absolute;
    height: 100%;
    width: 100%;
    z-index: -1;
    filter: blur(6px);
  } */
  .project-card {
    position: relative;
    border-radius: 4px;
    background-color: var(--project-background-color);
    align-items: center;
    justify-content: space-between;
    height: 100%;
    display: flex;
    /* box-shadow: 0 4px 8px 0 rgba(255, 255, 255, 0.2); */
    box-shadow:
      #487ce4 0px 0px 2px,
      #fffccc 0px 0px 4px;
    gap: 2rem;
    padding: 1.5rem 1rem;
    width: 100%;
    max-width: 800px;
    flex-direction: column;
    z-index: 50;
    transform: translateY(50px) scale(0.5);
    transition: transform 0.3s ease-in;
  }
  .active {
    transform: translateY(0px) scale(1);
  }

  .project-card main {
    flex-grow: 1; /* Para que las imágenes no queden en diferentes alturas */
  }

  /* Box Shadow Gradient */

  footer {
    width: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 18px;
  }
  .project-title {
    /* color: #ef652a; */
    color: rgb(0, 255, 255);
    font-size: 1.4rem;
    text-align: center;
  }
  .project-link {
    position: relative;
    text-align: center;
    /* display: inline-flex; */
    /* justify-content: center; */
    /* align-items: center; */
    /* max-width: 180px; */
    padding: 4px 14px;
    /* height: 40px; */
    /* border-radius: 6px; */
    font-size: 1.2rem;
    text-decoration: none;
    font-weight: 700;
    /* color: rgb(255, 43, 160); */
    color: #f2aa4c;
  }

  @media (hover: hover) {
    .project-link:hover {
      text-decoration: underline;
      color: #61dafb;
      /* background-image: linear-gradient(to right, #fff, #fff); */
    }
    .project-link::after {
      position: absolute;
      content: '🚀️';
      width: max-content;
      /* display: none; */
      opacity: 0;
      /* color: #fff; */
      /* color: #111; */
      transition: opacity 0.2s ease-in;
    }
    .project-link:hover::after {
      /* display: inline-block; */
      opacity: 1;
      /* background-color: #fff1; */
      /* right: -15px;
      animation-iteration-count: 1; */
    }
  }
  .project-link:active {
    transform: scale(0.99);
    color: var(--secondary-color-button);
  }
  a {
    color: #ddd;
  }
  /* a:hover {
    text-decoration: underline;
  } */

  main {
    width: 100%;
  }
  p {
    margin: auto;
    width: 100%;
    text-align: center;
    padding: 0 10px;

    line-height: 1.5;
  }
  .image-container {
    width: 250px;
    border-radius: 5px;
    contain: content;
    min-width: 250px;
    min-height: 250px;
    font-size: 60px;
    background-color: #09f8;

    display: flex;
    /* justify-self: flex-end; */
    align-items: center;
    justify-content: flex-end;

    z-index: -1;
  }
  /* li {
    display: flex;
    max-width: 900px;
    justify-items: center;
    align-items: center;
  } */

  @media (max-width: 820px) {
    .project-card {
      border-radius: 0px;
      border-right-color: transparent;
      border-left-color: transparent;
      border-top-color: #487ce4a0;
      border-bottom-color: #487ce4a0;
      border-width: 0px;
      border-top-width: 1px;
      border-bottom-width: 1px;
    }
  }
  h3 {
    color: var(--secondary-color);
  }
  @media (min-width: 480px) {
    footer {
      flex-direction: row;
    }
    p {
      text-align: justify;
      max-width: 80ch;
    }
  }
</style>
